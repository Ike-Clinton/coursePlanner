#content
%h1 The Student View Page
%p
Welcome #{current_user.name}, here are the classes you have taken

%table#classes
  %head
    %tr
      %th Class
      %th CRN
  %tbody
    - @selected.each do |classItem|
      %tr
        %td= classItem.class_name
        %td= classItem.crn
%p
And these are the classes you need to take to graduate: