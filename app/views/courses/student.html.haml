#content
%h1 The Student View Page
%p
Welcome #{current_user.name}, if you have not registered your classes, please click
= link_to 'Here', '/register_classes'

%p
Here are the classes you have taken

.container-fluid
  .row
    .panel
      .col-xs-2 Class Name
      .col-xs-2 Course Number
      .col-xs-2 Pre-Requisites
  - @classes_taken.each do |classItem|
    .row
      .panel
        .col-xs-2= classItem.class_name
        .col-xs-2= classItem.crn
        .col-xs-2= classItem.pre_reqs
      
%p
And these are the classes you need to take to graduate:
.container-fluid
  .row
    .panel
      .col-xs-2 Class Name
      .col-xs-2 Course Number
      .col-xs-2 Pre-Requisites
  - @classes_required.each do |classItem|
    .row
      .panel
        .col-xs-2= classItem.class_name
        .col-xs-2= classItem.crn
        .col-xs-2= classItem.pre_reqs
        
%p
  %h1 Comments
  %table#comments
  %p
  %tbody
  - @user.comments.each do |comment|
    %div
      %strong From:
      %strong= comment.from_user
      %br/
      %p
        = comment.body
      %p
%strong Add a Comment
= render 'comments/form'