#content
%h1 The Student View Page
%p
Welcome #{current_user.name}, if you have not registerd your classes, please click
= link_to 'Here', '/register_classes'

%p
Here are the classes you have taken

%table#classes
  %head
    %tr
      %th Class    
      %th Class Number
  %tbody
    - @classes_taken.each do |classItem|
      %tr
        %td= classItem.class_name
        %td= classItem.crn
%p
And these are the classes you need to take to graduate:

%table#classes_required
  %head
    %tr
      %th Class    
      %th Class Number
  %tbody
    - @classes_required.each do |classItem|
      %tr
        %td= classItem.class_name
        %td= classItem.crn
        
  %h2 Comments
  - @user.comments.each do |comment|
    %div
      %strong= comment.from_user
      %br/
      %p
        = comment.body
      %p
= render 'comments/form'
%td= link_to 'Delete', '/comment', method: :delete, data: { confirm: 'Are you sure?' }
      
= render 'comments/form'